mutation ChangePassword($newPassword: String!, $newPasswordConfirmed: String!, $token: String!){
    changePassword(newPassword: $newPassword, newPasswordConfirmed: $newPasswordConfirmed, token: $token){
        ...RegularUserResponse
    }
}